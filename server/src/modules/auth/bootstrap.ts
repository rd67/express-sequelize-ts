// import { logInfo } from '../../common';

// import * as constants from "./constants";

export const bootstrapSettings = async () => {
  // const settingsCount = await models.Settings.countDocuments();
  // if (!settingsCount) {
  //   logInfo({
  //     msg: 'BootStraping Settings',
  //   });
  //   await models.Settings.create({
  //     appVersioning: constants.DefaultAppVersioning,
  //   });
  // }
};
